version: '3.8'
services:
  map-screenshot-service:
    build: .
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./public:/app/public

  leaflet-map-viewer:
    build:
      context: ./leaflet-map-viewer
      dockerfile: Dockerfile
    restart: unless-stopped
